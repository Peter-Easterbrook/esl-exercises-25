# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
google-play-key.json
# Expo
.expo/
dist/
web-build/
expo-env.d.ts
credentials.json
# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
  .env
  .env.local
  .env.*.local

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

# Security - NEVER commit credentials or sensitive data
admin.txt
credentials.txt
*.env
.env
.env.local
.env.production
Google.json
*.pdf

OAUTH_TROUBLESHOOTING.md
null

# EAS environment dumps. Everything in here today is EXPO_PUBLIC_ and therefore
# public anyway, but this is the kind of file that quietly acquires a real
# secret (the .env also holds GOOGLE_CLIENT_SECRET). Keep it out of a public repo.
production.json
*.env.json
